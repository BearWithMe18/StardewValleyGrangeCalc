import { Component } from '@angular/core';
import { App } from '../app';

@Component({
  selector: 'app-score',
  imports: [App],
  templateUrl: './score.html',
  styleUrl: './score.css'
})
export class Score {
}
